# ── Core ──────────────────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
pyyaml>=6.0.1
rich>=13.7.1               # CLI formatting / pretty output
typer>=0.12.3              # CLI entry point
httpx>=0.27.0              # Async HTTP (used by many SDKs)
aiofiles>=23.2.1

# ── LLM Providers ─────────────────────────────────────────────────────────────
openai>=1.35.0                        # OpenAI + Codex + Azure-compat
anthropic>=0.28.0                     # Anthropic API + Claude CLI compat
google-generativeai>=0.7.0            # Gemini
groq>=0.9.0                           # Groq
mistralai>=1.0.0                      # Mistral
boto3>=1.34.0                         # Amazon Bedrock
cohere>=5.5.0                         # (optional, Bedrock route too)

# OpenRouter / Chutes / Venice / Z.AI / BytePlus / Vercel AI Gateway
# → all use OpenAI-compatible REST, no extra SDK needed (httpx)

# xAI (Grok) — OpenAI-compatible, no extra SDK
# fal — image/video inference
fal-client>=0.4.2

# Fireworks AI — OpenAI-compatible
fireworks-ai>=0.15.0

# Cloudflare AI Gateway — proxied via httpx

# Runway — media generation
runwayml>=0.1.0

# GLM / ChatGLM (Zhipu AI)
zhipuai>=2.1.0

# MiniMax
# → REST only, httpx

# Moonshot AI (Kimi)
# → OpenAI-compatible, httpx

# StepFun
# → OpenAI-compatible, httpx

# Qianfan (Baidu)
qianfan>=0.3.10

# Qwen / Alibaba Model Studio (DashScope)
dashscope>=1.20.0

# Ollama (local)
ollama>=0.2.1

# DeepSeek — OpenAI-compatible, httpx

# ComfyUI — local REST, httpx

# Vercel AI Gateway — OpenAI-compatible, httpx

# ── Memory ────────────────────────────────────────────────────────────────────
chromadb>=0.5.3            # Vector store (local, no server needed)
sentence-transformers>=3.0.1  # Embeddings
tiktoken>=0.7.0            # Token counting

# ── Skills ────────────────────────────────────────────────────────────────────
duckduckgo-search>=6.2.1   # Web search
sympy>=1.12.0              # Symbolic math / calculator

# ── Vision ────────────────────────────────────────────────────────────────────
Pillow>=10.4.0
pytesseract>=0.3.10        # OCR fallback

# ── Channels ──────────────────────────────────────────────────────────────────
python-telegram-bot>=21.4  # Telegram (async)
# whatsapp: twilio or meta cloud api — added when channel is activated

# ── Dev / optional ────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7
